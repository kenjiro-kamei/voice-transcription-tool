# ===========================================
# Production Environment Variables
# ===========================================
# Copy this file to .env.production and fill in actual values
# NEVER commit .env.production to version control
#
# Usage:
#   cp .env.production.example .env.production
#   # Edit .env.production with actual values
#   docker compose -f docker-compose.prod.yml up -d

# -------------------------------------------
# Database (PostgreSQL)
# -------------------------------------------
# Neon PostgreSQL (Recommended)
# Format: postgresql://user:password@host:port/dbname?sslmode=require
DATABASE_URL=postgresql://user:password@your-neon-host.neon.tech:5432/transcription_db?sslmode=require

# For local Docker PostgreSQL (development only)
# DATABASE_URL=postgresql://transcription:transcription_password@postgres:5432/transcription_db

# -------------------------------------------
# OpenAI API
# -------------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# -------------------------------------------
# Cloudflare R2 (Object Storage)
# -------------------------------------------
# Get credentials from Cloudflare Dashboard > R2 > Manage R2 API Tokens
R2_ACCOUNT_ID=your-cloudflare-account-id
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_BUCKET_NAME=voice-transcription-prod

# -------------------------------------------
# Redis
# -------------------------------------------
# Redis Cloud (Recommended)
# Format: redis://default:password@host:port
REDIS_URL=redis://default:your-redis-password@your-redis-host.redis-cloud.com:12345

# For local Docker Redis (development only)
# REDIS_URL=redis://redis:6379

# -------------------------------------------
# Application Settings
# -------------------------------------------
# Frontend URL (for CORS and redirects)
FRONTEND_URL=https://your-domain.com

# Backend API URL
BACKEND_URL=https://api.your-domain.com

# CORS allowed origin (usually same as FRONTEND_URL)
CORS_ORIGIN=https://your-domain.com

# Environment: development, staging, production
ENVIRONMENT=production

# -------------------------------------------
# Docker-specific Variables
# -------------------------------------------
# Only needed if using local PostgreSQL with Docker
POSTGRES_USER=transcription
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_DB=transcription_db

# Vite API URL (for frontend build)
VITE_API_URL=https://api.your-domain.com

# -------------------------------------------
# Optional: Logging & Monitoring
# -------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR
# LOG_LEVEL=INFO

# Sentry DSN (for error tracking)
# SENTRY_DSN=https://your-sentry-dsn

# -------------------------------------------
# Security Notes
# -------------------------------------------
# 1. Use strong, unique passwords for all services
# 2. Rotate API keys and secrets regularly
# 3. Use secrets management in production:
#    - AWS Secrets Manager
#    - Google Secret Manager
#    - HashiCorp Vault
# 4. Enable SSL/TLS for all external connections
# 5. Set appropriate CORS origins (not *)
